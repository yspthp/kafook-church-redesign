/**
 * 迦福堂 網站版面別名元件 (SiteLayout Alias Component)
 * 重新導出 Layout，確保無論使用 Layout 或 SiteLayout、具名或預設匯出皆完全相容
 */

import { Layout, SiteLayout } from './Layout.js';

export { Layout, SiteLayout };
export default SiteLayout;
